import { ShortpressDirective } from './shortpress.directive';

describe('ShortpressDirective', () => {
  it('should create an instance', () => {
    const directive = new ShortpressDirective();
    expect(directive).toBeTruthy();
  });
});
